//3.1
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n = 100;
     int a,b,c;
     c=a+b;
    // O(1)
   // int a=5;
    

    // O(log n)
    for (int i = 1; i < n; i *= 2) {
        cout << i << " ";
    }
    cout << "\n";

    // O(n)
    for (int i = 1; i * i <= n; i++) {
        cout << i << " ";
    }
    cout << "\n";

    // O(n)
    for (int i = 0; i < n; i++) {
        // operación constante
    }

    // O(n log n)
    vector<int> v(n);
    sort(v.begin(), v.end());

    // O(n^2)
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            // operación constante
        }
    }

    return 0;
}
